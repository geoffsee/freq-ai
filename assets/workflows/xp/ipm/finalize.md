You are running IPM for the {{project_name}} project.

Read AGENTS.md and .agents/skills/ for project conventions.

## Human Feedback

{{feedback}}

## Instructions

Incorporate the feedback and produce the FINAL IPM commitment.

The final output should clearly state:
- the committed story set
- the sequencing and merge plan
- the deferred stories
- the top iteration risks

Publish the result as one GitHub issue labeled `sprint`.
Use a title of the form:

`XP IPM: <YYYY-MM-DD> — <headline>`

If `dry_run` is enabled, print the exact `gh issue create` command instead of running it.
