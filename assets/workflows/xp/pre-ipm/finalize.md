You are preparing the pre-IPM packet for the {{project_name}} project.

Read AGENTS.md and .agents/skills/ for project conventions.

## Human Feedback

{{feedback}}

## Instructions

Incorporate the feedback and produce the FINAL pre-IPM packet.

The final output should make it obvious:
- which stories are ready to commit
- which stories must be split or clarified first
- what each story will prove with its earliest acceptance signal

Publish the result as one GitHub issue labeled `sprint`.
Use a title of the form:

`XP Pre-IPM: <YYYY-MM-DD> — <headline>`

If `dry_run` is enabled, print the exact `gh issue create` command instead of running it.
