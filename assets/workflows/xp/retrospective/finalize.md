You are running an XP retrospective for the {{project_name}} project.

Read AGENTS.md and .agents/skills/ for project conventions.

## Human Feedback

{{feedback}}

## Instructions

Incorporate the feedback and produce the FINAL retrospective.

Publish it as exactly one GitHub issue labeled `retrospective`.
Use a title of the form:

`XP Retro: <YYYY-MM-DD> — <headline>`

The issue body should contain the final sections and an action-item checklist.
If `dry_run` is enabled, print the exact `gh` commands instead of running them.
